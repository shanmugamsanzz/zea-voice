import {withTenantContext} from '../infrastructure/database-context.js';import {AppError} from '../middleware/errors.js';import {encryptCredential} from '../security/credential-crypto.js';import {validateToolHeaders,validateWebhookEndpoint} from '../voice/tools/tool-security.js';import {assertAgentWorkflowToolsRemainAssigned} from '../knowledge-bases/workflow-tool-authorization.js';
function tool(row){return{id:row.id,agentId:row.agent_id,name:row.name,type:row.type,description:row.description,status:row.status,configuration:row.configuration,hasSecretConfiguration:Boolean(row.secret_configuration_encrypted),createdAt:row.created_at,updatedAt:row.updated_at};}
async function ensureToolAgent(client,auth,agentId){const r=await client.query('SELECT id FROM voice_agents WHERE tenant_id=$1 AND workspace_id=$2 AND id=$3 AND deleted_at IS NULL',[auth.tenantId,auth.workspaceId,agentId]);if(!r.rowCount)throw new AppError(404,'Voice agent was not found','AGENT_NOT_FOUND');}
export function listTools(auth,agentId){return withTenantContext(auth,async c=>{await ensureToolAgent(c,auth,agentId);const r=await c.query('SELECT * FROM agent_tools WHERE tenant_id=$1 AND workspace_id=$2 AND agent_id=$3 AND deleted_at IS NULL ORDER BY created_at',[auth.tenantId,auth.workspaceId,agentId]);return r.rows.map(tool);});}
export function createTool(auth,agentId,input){return withTenantContext(auth,async c=>{await ensureToolAgent(c,auth,agentId);if(input.type==='webhook_api'){input.configuration.url=await validateWebhookEndpoint(input.configuration.url,{resolveDns:false});validateToolHeaders(input.configuration.headers);const secretHeaders=input.secretConfiguration?.headers??input.secretConfiguration??{};validateToolHeaders(secretHeaders,{secret:true});}try{const r=await c.query(`INSERT INTO agent_tools(tenant_id,workspace_id,agent_id,name,type,description,status,configuration,secret_configuration_encrypted,created_by)VALUES($1,$2,$3,$4,$5,$6,$7,$8::jsonb,$9,$10)RETURNING *`,[auth.tenantId,auth.workspaceId,agentId,input.name,input.type,input.description??null,input.status,JSON.stringify(input.configuration),input.secretConfiguration?encryptCredential(JSON.stringify(input.secretConfiguration)):null,auth.userId]);return tool(r.rows[0]);}catch(e){if(e.code==='23505')throw new AppError(409,'Tool name already exists for this agent','AGENT_TOOL_EXISTS');throw e;}});}
export function updateTool(auth,agentId,id,input){return withTenantContext(auth,async c=>{await ensureToolAgent(c,auth,agentId);if(input.type==='webhook_api'){input.configuration.url=await validateWebhookEndpoint(input.configuration.url,{resolveDns:false});validateToolHeaders(input.configuration.headers);if(input.secretConfiguration){const secretHeaders=input.secretConfiguration.headers??input.secretConfiguration;validateToolHeaders(secretHeaders,{secret:true});}}try{const encryptedSecret=input.secretConfiguration?encryptCredential(JSON.stringify(input.secretConfiguration)):null;const r=await c.query(`UPDATE agent_tools SET name=$5,type=$6,description=$7,status=$8,configuration=$9::jsonb,secret_configuration_encrypted=CASE WHEN $10::text IS NULL THEN secret_configuration_encrypted ELSE $10 END WHERE tenant_id=$1 AND workspace_id=$2 AND agent_id=$3 AND id=$4 AND deleted_at IS NULL RETURNING *`,[auth.tenantId,auth.workspaceId,agentId,id,input.name,input.type,input.description??null,input.status,JSON.stringify(input.configuration),encryptedSecret]);if(!r.rowCount)throw new AppError(404,'Agent tool was not found','AGENT_TOOL_NOT_FOUND');await assertAgentWorkflowToolsRemainAssigned(c,{tenantId:auth.tenantId,agentId});return tool(r.rows[0]);}catch(e){if(e.code==='23505')throw new AppError(409,'Tool name already exists for this agent','AGENT_TOOL_EXISTS');throw e;}});}
export function updateToolStatus(auth,agentId,id,status){return withTenantContext(auth,async c=>{await ensureToolAgent(c,auth,agentId);const r=await c.query('UPDATE agent_tools SET status=$5 WHERE tenant_id=$1 AND workspace_id=$2 AND agent_id=$3 AND id=$4 AND deleted_at IS NULL RETURNING *',[auth.tenantId,auth.workspaceId,agentId,id,status]);if(!r.rowCount)throw new AppError(404,'Agent tool was not found','AGENT_TOOL_NOT_FOUND');await assertAgentWorkflowToolsRemainAssigned(c,{tenantId:auth.tenantId,agentId});return tool(r.rows[0]);});}
export function deleteTool(auth,agentId,id){return withTenantContext(auth,async c=>{await ensureToolAgent(c,auth,agentId);const r=await c.query('UPDATE agent_tools SET deleted_at=now(),status=\'inactive\' WHERE tenant_id=$1 AND workspace_id=$2 AND agent_id=$3 AND id=$4 AND deleted_at IS NULL RETURNING id',[auth.tenantId,auth.workspaceId,agentId,id]);if(!r.rowCount)throw new AppError(404,'Agent tool was not found','AGENT_TOOL_NOT_FOUND');await assertAgentWorkflowToolsRemainAssigned(c,{tenantId:auth.tenantId,agentId});return{id,deleted:true};});}
