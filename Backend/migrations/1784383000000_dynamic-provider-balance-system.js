// Historical compatibility marker. The dynamic provider-balance experiment
// is retired, but deployed databases already contain this migration name.
export const shorthands = undefined;
export async function up() {}
export async function down() {}
